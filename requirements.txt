# Ghost Media Engine - Dependencies
# Core
fastapi>=0.109.1
uvicorn[standard]==0.24.0
python-dotenv==1.0.0

# HTTP
requests>=2.32.0
aiohttp==3.9.1
httpx>=0.25.0

# Browser Automation
playwright==1.40.0
selenium>=4.15.0
websocket-client>=1.7.0

# LLM / AI
google-generativeai>=0.3.0
transformers>=4.40.0
torch>=2.1.0
accelerate>=0.27.0

# Discord
discord.py==2.3.2

# Security
cryptography>=44.0.0
slowapi==0.1.9

# Deployment
gradio==3.50.0
huggingface-hub>=0.19.0
google-auth==2.27.0
google-auth-oauthlib==1.2.0
google-api-python-client==2.116.0

# Decentralized Infrastructure (Unstoppable Layer)
# IPFS — pip install ipfshttpclient (requires IPFS daemon)
# Arweave — permanent storage via REST API (no SDK needed)
liboqs-python>=0.10.0      # Post-quantum cryptography (Kyber1024 / ML-KEM)
                               # C library: apt install liboqs-dev

# Utilities
webdriver-manager>=4.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
pytest-timeout>=2.2.0
fakeredis>=2.20.0           # Redis mock for testing
